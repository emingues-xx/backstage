# Variáveis de ambiente para Railway
# Configure estas variáveis no painel do Railway

# URLs da aplicação (IMPORTANTE: Configure com o domínio real do Railway)
# Exemplo: https://backstage-production.up.railway.app
APP_BASE_URL=https://backstage-production.up.railway.app
BACKEND_BASE_URL=https://backstage-production.up.railway.app
PORT=3000

# Segurança - GERE UMA CHAVE SEGURA!
BACKEND_SECRET=my-super-secret-key-for-production-2024-change-this

# GitHub OAuth (obrigatório para autenticação)
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_TOKEN=your-github-token-here

# PostgreSQL - Use as variáveis automáticas do Railway:
# IMPORTANTE: Host correto do Railway
POSTGRES_HOST=metro.proxy.rlwy.net
POSTGRES_PORT=38211
POSTGRES_USER=postgres
POSTGRES_PASSWORD=HMyJaLtRmOWbyDEkAZLPphTzTsKcMGVF
POSTGRES_DB=railway

# Variáveis adicionais necessárias
NODE_ENV=production
NODE_OPTIONS=--no-node-snapshot
NODE_VERSION=20.18.0
NIXPACKS_NODE_VERSION=20.18.0
NIXPACKS_NODE_MAJOR_VERSION=20

# Forçar migrações do banco
BACKSTAGE_DATABASE_MIGRATIONS=true