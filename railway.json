{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "yarn workspace backend build && yarn workspace backend start --config app-config.yaml --config app-config.production.yaml",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300
  }
}